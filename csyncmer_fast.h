#include <stddef.h>

// wrap functions to compute closed syncmers
typedef struct syncmerIterator
{
    
};

syncmerIterator syncmerIteratorInitialization(int K, int S, char *sequence){
    // allocate memory
    // syncmerIterator *si = 

    // initialize variables

    // initialize hash function

    // precompute first syncmer

    // return si;
}

void get_next_syncmer( size_t *kmer_pos, size_t *smer_pos){
    // compute next syncmer

    // output

}